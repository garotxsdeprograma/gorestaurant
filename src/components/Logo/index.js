import React from "react";
import * as S from "./style";


const Logo = (props, { placeholder, type }) => {
    return (
        <>
            <S.ContainerInput>
            </S.ContainerInput>
        </>
    );
};

export default Logo;