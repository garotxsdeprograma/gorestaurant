import React from "react";
import * as S from "./style";


const Input = (props, { placeholder, type }) => {
    return (
        <>
            <S.ContainerInput>
            </S.ContainerInput>
        </>
    );
};

export default Input;
